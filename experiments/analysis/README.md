# Analysis

This directory contains analysis scripts and outputs that summarize or interpret experiment behavior rather than train a new benchmark model.

- `correct_set_overlap`: overlap analysis comparing IQ, FFT, and fusion predictions
