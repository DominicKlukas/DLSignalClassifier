# Experiment Tools

Utilities used to support experiments and documentation:

- `generate_dataset_figures.py`: builds figures used by the dataset documentation

Dataset retrieval and conversion scripts now live under `dataset_prep/`.
